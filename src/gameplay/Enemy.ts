// Represents an enemy actor in the dungeon.
// TODO: Support different enemy archetypes, AI behaviors, and loot tables.

export class Enemy {
  update(deltaTime: number): void {
    void deltaTime;
    // TODO: Update enemy AI logic and interactions with the player.
  }
}
