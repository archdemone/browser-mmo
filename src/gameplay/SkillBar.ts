// Represents the player's hotbar slots mapped to abilities.
// TODO: Allow assigning learned skills to specific slots and track cooldown readiness.
// TODO: When slots are activated (keyboard/mouse), call AbilitySystem to trigger abilities and manage cooldown UI states.

export class SkillBar {
  // TODO: Maintain mapping between slot indices and skill identifiers.
  activateSlot(slotIndex: number): void {
    void slotIndex;
    // TODO: Relay activation to AbilitySystem and enforce cooldown timing.
  }
}
